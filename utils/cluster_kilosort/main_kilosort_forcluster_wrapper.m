function main_kilosort_forcluster_wrapper(input_folder, config_folder

% paths to add
% main rep
% utils with npy, KS git submodule, KS cluster
% configs
% where the raw data is

% call main_kilosort_fx_cluster(input_folder, config_folder

end