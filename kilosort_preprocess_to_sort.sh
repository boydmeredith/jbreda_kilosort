# copy and paste bash heading here, but add GPU extension (see Emily's cscope for more info

# need to figure out bundle organization

# preprocess data, will create a new folder
# add that folder to the path

# run kilosort form that folder, have kilosort take config file from somewhere else rather than one config per .bin
